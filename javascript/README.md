# RetroStore JavaScript SDK

To compile, install TypScript:

```shell
$ npm install typescript --save-dev
```

To compile into the demo folder:

```shell
$ npx tsc retrostore_api.ts --outFile ./demo/retrostore_api.js
```